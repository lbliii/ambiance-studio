# Coordinated implementation — third wave

The user accepted four next-wave tasks after [PR #33](https://github.com/lbliii/ambiance-studio/pull/33) merged Wave 2 into main at `75d24b85a8977f234d87daa1c18993d394036c1c`. The clean saved-project main checkout is fast-forwarded to this baseline. Its exact tree passed 516 recorded cases / 501 Python tests / zero skips; later completion-only documents were audited. GitHub jobs did not execute because billing/spending limits blocked them.

The [live ledger](COORDINATED-IMPLEMENTATION-WAVE3-2026-09-13.json) owns dispatch IDs, branches, interfaces, shared-file leases, test slots, integration and validation. The coordinator uses `codex/coordinated-studio-wave3`; four fresh tasks use isolated worktrees from main. Existing wave tasks remain complete. Read [studio alignment](STUDIO-PLAN-ALIGNMENT.md) first; owning plans retain their own acceptance statuses.

| Stream | Assignment | Result and boundary |
| --- | --- | --- |
| W3 | [WF-04/05](WORKFLOW-CLI.yaml) | Immutable valid native input packets and opt-in bounded outcome guidance. Preserve successful mutations even if guidance fails. Stop before WF-06/07 adoption. |
| M3 | [I1/I2](model-contract/implementation-v1.md) | Two independent contained model instances, restorable explicit version adoption and typed evidence. Stop before representative/full-scene artistic acceptance. |
| T3 | [P03](FIRST-SHORT-WORK-PACKAGES.json) | Shared finite-shot/local-cycle clocks, exact conversions and native endpoint proof, preserving legacy loops. Stop before cues, sequence assembly and film-level evidence. |
| A3 | [AUDIO-02](TV-QUALITY-AND-SOUND-LIBRARY.yaml) | Explicit LUFS/true-peak measurements and export audio bitrate, with decoded AAC evidence. Stop before curation, mixing and TV profiles. |

## Shared interfaces

The integrated mc/1 packet is the bounded technical prerequisite for P03. Full Phase 1/P00 seed/story/census and real-art acceptance remain open. M3 first probes actual C1/C2 lantern lowering and uses current scene placement/transactions. T3 alone owns evaluator/time conversion; M3 consumes that API. M3 can implement the static instance path without waiting for finite timing, then validates combined compatibility.

W3 owns optional global guidance around native outcomes; it preserves existing output ownership, exit/error behavior, exact subjects and no-side-effect reads. M3/T3/A3 own narrow new operation registrations and return their hook patches for reconciliation. The coordinator owns broad CLI/backlog/capability changes.

A3 owns audio codec/bitrate/measurement fields; T3 owns picture clock/raster/receipt timing. Both provide exact function/field maps before shared renderer/receipt integration. No second evaluator, renderer, census, implicit source upgrade or invented observation is introduced.

## Execution and validation

Each task reports its actual thread ID, worktree, branch, verified base and first interface proposal to coordinator task `01a0983d-40cf-71a3-bd9c-cb8308ad6efa`. Setup client IDs are not real thread IDs. The ledger retains both and no duplicate task is created because an inventory omits a new task.

Use real public CLI probes/replays and existing shared validators. Preserve source/artifact/report hashes and actual observations. One committed frozen-source full native suite runs at a time; request the slot through the coordinator and continue focused independent work while queued. Local loopback/Core Audio/AVFoundation access is necessary; restricted failures remain diagnostic and are not counted as passes. Packaging changes need the package audit.

The coordinator reviews source and exact handoffs, supplies missing interfaces, integrates focused commits in dependency order and runs one combined required-native suite on the stable tree. Story/art/voice/film and broader follow-up work remain outside this kickoff. Native technical evidence never establishes artistic acceptance or a human audition.

The existing 15-minute heartbeat resumes for these four tasks and reads the ledger. It remains quiet on unchanged/non-actionable state, reports meaningful progress or a real blocker, and pauses after reviewed local integration, combined validation and the handoff. Future PR/main merge follows the user's then-current direction; no account/protection changes are authorized.
